[copy]
    includeDirs = .claude
    includeDirs = .rulesync

[hooks]
    postCreate = source ~/.bashrc && mise trust && mise install && pnpm i
    postCd = source ~/.bashrc

[defaults]
    editor = zed
    ai = claude
